{
  "name": "@rebuildingamerica/atlas-api",
  "private": true,
  "scripts": {
    "dev": "portless api.atlas --force uv run python -m atlas.main",
    "typecheck": "uv run mypy atlas",
    "lint": "uv run ruff check atlas",
    "test": "uv run --extra dev pytest -n auto --dist loadfile tests"
  },
  "devDependencies": {
    "portless": "catalog:"
  },
  "dependencies": {
    "@rebuildingamerica/atlas-discovery-engine": "workspace:*",
    "@rebuildingamerica/atlas-shared": "workspace:*"
  }
}
